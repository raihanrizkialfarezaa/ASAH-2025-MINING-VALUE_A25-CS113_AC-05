import React from 'react';

const MarkerClusterGroup = ({ children, chunkedLoading, maxClusterRadius, spiderfyOnMaxZoom, showCoverageOnHover }) => {
  return <>{children}</>;
};

export default MarkerClusterGroup;
